<% nginx_global_options_real.each do | key, val | -%>
<%= key %> <%= val %>;
<% end -%>

events {
<% nginx_events_options_real.each do | key, val | -%>
  <% key %> <%= val %>;
<% end -%>
}

http {
<% nginx_http_options_real.each do | key, val | -%>
  <% key %> <%= val %>;
<% end -%>
}
